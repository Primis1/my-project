# 1. Redirect all HTTP to HTTPS
server {
    listen 80;
    server_name babinskyi.ca www.babinskyi.ca n8n.babinskyi.ca;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}

# 2. Payload CMS (Main Site)
server {
    listen 443 ssl;
    server_name babinskyi.ca www.babinskyi.ca;
    client_max_body_size 20M;

    ssl_certificate /etc/letsencrypt/live/babinskyi.ca/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/babinskyi.ca/privkey.pem;

    location / {
        proxy_pass http://payload:3000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# 3. n8n Automation Dashboard
server {
    listen 443 ssl;
    server_name n8n.babinskyi.ca;

    ssl_certificate /etc/letsencrypt/live/babinskyi.ca/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/babinskyi.ca/privkey.pem;
    large_client_header_buffers 4 16k;

    location / {
        proxy_pass http://n8n:5678;
        proxy_set_header Host $host;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_buffering off;
        
        # Ensure the proxy also handles larger headers
        proxy_buffer_size 128k;
        proxy_buffers 4 256k;
        proxy_busy_buffers_size 256k;
    }
}